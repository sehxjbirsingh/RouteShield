services:
  - type: web
    name: routeshield
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir backend app:app
    envVars:
      - key: GEMINI_API_KEY
        sync: false